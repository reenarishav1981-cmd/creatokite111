# ── Dev (Vite proxy handles /api) ─────────────
# Leave empty — vite.config.js proxy does the work
VITE_API_URL= https://creatokite111.onrender.com/api

# ── Production — set your backend URL ─────────
# VITE_API_URL=https://creatokite-backend.onrender.com/api
